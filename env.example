# Render
ALLOWED_ORIGINS=https://zayaiken21.github.io
# Optional: reserve ADMIN_TOKEN for future MCP/admin-only endpoints. Do not put it in GitHub Pages.
ADMIN_TOKEN=
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6
# V11 image generation (used when a post's images are missing from the ZIP)
OPENAI_IMAGE_MODEL=gpt-image-1
# Optional: only for testing against an OpenAI-compatible endpoint
OPENAI_BASE_URL=
